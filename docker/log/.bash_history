ping redis
host redis
set
bundle exec sidekiq
set
host redis
ping redis
apt-get install ping
apt-get install inet-utils
nc
redis-cli
ls -la
cat Gemfile
cat Gemfile.lock 
rails c
cd /usr/local/lib/ruby/site_ruby/
l
ll
ls -la
cd 2.6.0/
ls -la
cd bundler
ls -la
cd
which bundler
bundler
bundler -v
cd /app
bundle
rails c
spring stop
bundle update --bundler
rails c
bundle exec rails c
bundle exec rails db:setup
set
bundle exec rails db:setup
set
apt install iputils-ping
apt-get install iputils-ping
apt-get update
apt-get install iputils-ping
ping redis
ping db
ECHO DATABASE_URL
echo $DATABASE_URL
bundle exec rails c
bundle exec rails db:schema:load
set
bundle exec rails db:schema:load
bundle exec rails db:schema:load
echo $RAILS_ENV
echo $DATABASE_YURL
echo $DATABASE_URL
bundle exec rails db:schema:load
bundle exec rails db:schema:load
echo $NODE_EBV
echo $NODE_ENV
echo $RAILS_ENV
echo $DATABASE_URL
echo $WEB_CONCURRENCY
echo $DATABASE_URL
bundle exec rails db:schema:load
bundle exec rails db:setup
bundle exec rails db:create
bundle exec rails db:setup
bundle exec rails db:setup
ping
bundle exec rails db:setup
bundle exec rails db:setup
bundle exec rails db:setup
bundle exec rails db:setup
